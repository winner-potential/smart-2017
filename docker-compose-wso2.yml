version: "2"

services:
  receiver:
    build:
      context: receiver
    restart: unless-stopped
    volumes:
      - wso2_receiver:/noderedtest:Z
    ports:
      - "1881:1880"
  test:
    build:
      context: wso2
    restart: unless-stopped
    volumes:
      - wso2_test:/wso2test:Z
    depends_on:
      - receiver
  sender:
    build:
      context: sender
    restart: unless-stopped
    volumes:
      - wso2_sender:/noderedtest:Z
    environment:
      INSTALLATION: test
      AMOUNT: 1
      ENDPOINT: http://test:9763/endpoints/SolarReceiver
    depends_on:
      - test
volumes:
  wso2_receiver:
    driver: local
  wso2_sender:
    driver: local
  wso2_test:
    driver: local